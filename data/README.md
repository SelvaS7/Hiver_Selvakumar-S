# Data

`apple_pairs.csv` is a 15,000-row development sample extracted from the supplied Customer Support on Twitter dataset for AppleSupport. It contains direct customer-to-AppleSupport reply pairs.

The original dataset is intentionally not included in this repository because it is large. Place the downloaded `twcs.csv` at `data/twcs.csv` and run the data-loader command in the root README.
